# URL da API Mentalize (backend Express)
# Em desenvolvimento com `npm run dev`, deixe vazio para usar o proxy do Vite (/api → localhost:3000)
VITE_API_URL=

# Em produção, aponte para o domínio do backend:
# VITE_API_URL=https://sua-api.onrender.com
